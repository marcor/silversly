{% load sales_tags %}

{% for item in cart.cartitem_set.all %}
<li>Prova</li>
<!--
    {% with total=item.net_total %}
    <tr>
        <td class="quantity right">{{ item.quantity }}{{ item.product.unit }}</td>
        <td class="description">{{ item.product.name }}</td>
        <td class="price">{{ item.final_net_price }}&euro;</td>
        <td class="item_total">{% if item.discount %}{{ total|sum }}{% else %}{{ total.0 }}{% endif %}&euro;</td>
    </tr>
    {% if item.discount %}   
    <tr>
        <td class="quantity"></td>
        <td class="description right">sconto art. {{ item.discount }}%</td>
         <td class="price"></td>
        <td class="item_total">-{{ total.1 }}&euro;</td>
    </tr>
    {% endif %}    
    {% endwith %}
    {% if cart.discount %}   
    
    {% endif %}
    -->
{% endfor %}
